# SQLite is embedded in the API process - no database service needed
# The database file is created at ./float.db (dev) or /app/data/float.db (Docker)
# See deploy/docker-compose.yml for the production setup
